{
  "name": "opencode-agents-v6",
  "version": "6.4.0",
  "description": "Multi-agent development harness for OpenCode v6",
  "private": true,
  "scripts": {
    "validate:syntax": "bash -n install.sh",
    "validate:types": "tsc --noEmit",
    "test": "node --experimental-strip-types --test tests/harness-tools.test.ts tests/harness-db.test.ts"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^7.0.2"
  }
}
